---
import { contactData } from '../config/navigation';

export interface Props {
  title?: string;
  subline?: string;
  image?: string;
  primaryBtnText?: string;
  secondaryBtnText?: string;
  primaryBtnLink?: string;
  phoneHref?: string;
  whatsappHref?: string;
}

const {
  title = "Freiraum-Auflösungen Berlin",
  subline = "Professionell, schnell & besenrein",
  image = "/images/placeholder-hero.jpg",
  primaryBtnText = "Jetzt anrufen",
  secondaryBtnText = "WhatsApp schreiben",
  primaryBtnLink,
  phoneHref = contactData.phoneHref,
  whatsappHref = contactData.whatsappHref
} = Astro.props;

const primaryLink = primaryBtnLink || phoneHref;
const secondaryLink = whatsappHref;
---

<section class="relative bg-primary text-white overflow-hidden">
  <div class="absolute inset-0 bg-black/50 z-10"></div>
  <div class="absolute inset-0 bg-gray-800 bg-[linear-gradient(to_right_bottom,rgba(0,0,0,0.6),rgba(0,0,0,0.3))] z-0"></div>

  <div class="container mx-auto px-4 py-20 md:py-32 relative z-20 text-center max-w-5xl">
    <h1 class="text-3xl md:text-5xl lg:text-6xl font-bold mb-6 leading-tight">
      {title}
    </h1>
    <p class="text-lg md:text-2xl mb-10 text-gray-100 max-w-3xl mx-auto font-light">
      {subline}
    </p>

    <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
      <a
        href={primaryLink}
        class="w-full sm:w-auto bg-accent hover:bg-blue-600 text-white font-bold py-4 px-8 rounded-lg transition-colors text-lg shadow-lg flex items-center justify-center gap-2 min-h-[48px]"
        data-event="phone_call"
        data-event-category="conversion"
      >
        <span>📞</span>
        <span>{primaryBtnText}</span>
      </a>

      <a
        href={secondaryLink}
        class="w-full sm:w-auto bg-transparent border-2 border-white text-white hover:bg-white hover:text-primary font-bold py-4 px-8 rounded-lg transition-colors text-lg flex items-center justify-center gap-2 min-h-[48px]"
        data-event="whatsapp_click"
        data-event-category="conversion"
        target="_blank"
        rel="noopener noreferrer"
      >
        <span>💬</span>
        <span>{secondaryBtnText}</span>
      </a>
    </div>
    <p class="mt-4 text-sm text-gray-300">✓ Kostenlos & unverbindlich · Antwort in 24h</p>
  </div>
</section>
